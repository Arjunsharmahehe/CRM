# CRM
This project uses `pnpm` as the package manager. To install `pnpm` run `npm install -g pnpm` to globally install it. 

- Install dependencies
```bash
pnpm install
```
- Run dev server
```bash
pnpm run dev
```
- Test build
```bash
pnpm built
```
- Check linting warnings
```bash
pnpm lint
```
